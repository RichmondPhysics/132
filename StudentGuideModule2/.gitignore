#this file excludes files we don't want as part of the repository.
**/*.aux
**/*.log
# with the ** wildcard, this should also exclude log and aux files from all subdirectories.  Why not working?
Thumbs.db
